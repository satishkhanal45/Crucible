# Copy to .env and fill in. .env is gitignored; never commit real secrets.

# Async SQLAlchemy URL. Inside docker compose the host is `postgres`; compose
# overrides this value for the api and migrate services.
DATABASE_URL=postgresql+asyncpg://crucible:crucible@localhost:5432/crucible

# Host ports published by docker compose. Change these if they are already taken.
POSTGRES_PORT=5432
API_PORT=8000

# dev | test | prod
ENV=dev
LOG_LEVEL=INFO

# LLM providers. Free tiers; see docs/spec.md section 1 for rules of engagement.
# Two providers, because a free tier's binding limit is a per-model token pool
# and a second host is a second pool. Both are required: a run may put one agent
# on each, and a missing key is a failure at startup rather than mid-run.
# DeepSeek's OpenAI-compatible endpoint (https://api.deepseek.com) was checked
# on 2026-09-04; see https://api-docs.deepseek.com for its current models.
GROQ_API_KEY=replace-me
DEEPSEEK_API_KEY=replace-me
GEMINI_API_KEY=replace-me

# Comma-separated hosts the executor is allowed to reach. Must not be empty.
TARGET_ALLOWLIST=localhost,127.0.0.1

# Per-round spend cap in USD, enforced by CostMeter.
ROUND_BUDGET_USD=5.00

EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Model ids. These are the ONLY place model ids are configured: no CLI option
# and no module constant carries one, so editing this file is what changes a
# run. Checked against Groq's catalogue on 2026-09-03 — Groq retires model ids
# on a schedule (llama-3.3-70b-versatile was decommissioned 2026-08-16), so
# check https://console.groq.com/docs/deprecations before a long run.
#
# Provider is chosen PER AGENT, paired with that agent's model: each may be
# `groq` or `deepseek`. This replaced the single LLM_PROVIDER setting when the
# second provider was added — spreading the four agents across two hosts is the
# point of having two, and one global setting could not say that. Setting all
# four to the same value reproduces the old behaviour exactly.
TARGET_PROVIDER=groq
TARGET_MODEL=llama-3.1-8b-instant
ATTACKER_PROVIDER=groq
ATTACKER_MODEL=openai/gpt-oss-120b
DEFENDER_PROVIDER=groq
DEFENDER_MODEL=openai/gpt-oss-120b
CLASSIFIER_PROVIDER=groq
CLASSIFIER_MODEL=openai/gpt-oss-120b

# DeepSeek model ids, for whichever agents you move there. These are what the
# models endpoint returned on 2026-09-05; the earlier deepseek-chat and
# deepseek-reasoner ids no longer exist:
#   deepseek-v4-flash, deepseek-v4-pro, deepseek-v4-flash-vision-exp
# Their prices in src/crucible/config.py are UNCONFIRMED placeholders — check
# https://deepseek.com/pricing before trusting a cost figure.

# Tier 3 judge. Not built in this cut (B4); the id is still validated for price.
JUDGE_PROVIDER=gemini
DEFAULT_JUDGE_MODEL=gemini-2.0-flash

# Pacing for provider calls, and the FALLBACK for any provider with no entry in
# PROVIDER_RATE_LIMITS / PROVIDER_CONCURRENCY below — set those, not these, to
# tune a provider. The limit that actually bites on Groq's free tier is TOKENS
# per minute (8000 for openai/gpt-oss-120b), not requests: stay below it,
# because the estimate that books the window is approximate and the reply length
# is unknown until it arrives. Requests per minute is the secondary cap.
# The minimum interval is per provider, and spaces that provider's calls only.
PROVIDER_MAX_CONCURRENCY=1
PROVIDER_MIN_INTERVAL_SECONDS=2.0
PROVIDER_TOKENS_PER_MINUTE=6500
PROVIDER_REQUESTS_PER_MINUTE=25

# PER-PROVIDER LIMITS. This is the normal way to set a rate limit, and the
# values here MUST MATCH YOUR ACCOUNT'S ACTUAL TIER — the code cannot know it,
# and a limit sized for the wrong tier is not a safety margin. A free-tier
# number on a paid key spends most of a round asleep in token-budget waits;
# a paid-tier number on a free key earns 429s.
#
# `provider=tokens_per_minute/requests_per_minute`, comma separated. Windows are
# keyed `provider:model`, so groq:X and deepseek:X never share one. Check
# the current limits at:
#   Groq     https://console.groq.com/docs/rate-limits
#   DeepSeek https://api-docs.deepseek.com/quick_start/rate_limit
# The values below are the developed-against defaults: Groq FREE tier, DeepSeek
# PAID. Raise or lower to match your own keys.
PROVIDER_RATE_LIMITS=groq=6500/25,deepseek=120000/300

# Concurrent in-flight calls per provider, `provider=n`. One is right for a free
# tier, where a second call in flight is a 429; a paid tier can overlap. The
# target pool is bounded by the TARGET provider's value, since a pool slot is a
# target caller.
PROVIDER_CONCURRENCY=groq=1,deepseek=4

# Per-provider, per-role read timeouts in seconds: `provider:role=seconds`, or
# `role=seconds` for every provider. Roles are target, attacker, defender,
# classifier. The defaults are in src/crucible/config.py and are generous for
# the defender, whose proposal prompt is the longest one the loop sends — a flat
# 60s read timeout there killed a run mid-experiment. The connect timeout stays
# short and is not configurable per role: a host that has not answered the
# handshake in ten seconds is unreachable, not slow.
# PROVIDER_READ_TIMEOUTS=deepseek:defender=300,groq:target=90

# Optional. `provider:model=prompt/completion` in USD per 1M tokens, comma
# separated, merged over the dated defaults in src/crucible/config.py. Set this
# when a provider changes its rates: without a price, spend records as NULL and
# ROUND_BUDGET_USD cannot be enforced.
# MODEL_PRICING=groq:openai/gpt-oss-120b=0.15/0.75,groq:llama-3.1-8b-instant=0.05/0.08
